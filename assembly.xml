<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 
                              http://maven.apache.org/xsd/assembly-2.2.0.xsd">

    <id>release</id>
    <formats>
        <format>zip</format>
    </formats>

    <!-- no extra top-level folder inside the ZIP -->
    <includeBaseDirectory>false</includeBaseDirectory>

    <!-- include the fat JAR built by the Shade plugin -->
    <files>
        <file>
            <source>target/yogi-bear-1.0.0.jar</source>
            <destName>yogi-bear.jar</destName>
        </file>
    </files>

    <!-- include the data folder (images, sounds, levels) -->
    <fileSets>
        <fileSet>
            <directory>data</directory>
            <outputDirectory>data</outputDirectory>
        </fileSet>
    </fileSets>

</assembly>